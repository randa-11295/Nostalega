import { useRouter  } from 'next/router'
import { useEffect  } from 'react'

const useCustomRoute = (newRoute) =>{


      const isOnline ="test"
    
      useEffect(() => {
          console.log(isOnline)
        }
    
      )
    
      return true;
    }


export default useCustomRoute
